- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.dotfiles_local: ''
    ~/.gitconfig_local:
    ~/.name:
    ~/.bashrc_local_before:
    ~/.shell_local_after:
    ~/.zshrc_local_before:

- shell:
  - git submodule sync --recursive
  - git update-submodules # after linking ~/.gitconfig
