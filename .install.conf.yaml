- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.dotfiles_local: ''
    ~/.gitconfig_local:
    ~/.name:
    ~/.shell_local_after:
    ~/.zshrc_local_before:

- shell:
  - git submodule sync --recursive
  - git update-submodules # after linking ~/.gitconfig
